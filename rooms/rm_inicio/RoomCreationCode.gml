//Exibindo as variaveis globais 

//show_message(global.max_pontos);
//show_message(global.total_inimigos);
//show_message(global.total_mortes);
//Iniciando musica
audio_stop_all();
audio_play_sound(snd_inicio2, 0, 1);

